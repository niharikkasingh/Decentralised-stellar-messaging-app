[package]
name = "messaging-contract"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "23.1.0"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"